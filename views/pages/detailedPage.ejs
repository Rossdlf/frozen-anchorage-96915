<!DOCTYPE html>
<html>
<head>
  <style>
      body{
          background-color: lightblue;
      }
        p {background-color:<%=rows[0].color %>;
        width:<%=rows[0].width %>px;
        height:<%=rows[0].length %>px;
        border:3px solid black;
        }
        ul {
            font-size: large;
            font-weight: bold;
        }
        input[type="submit"]{
            background-color: rgb(14, 191, 250);
            text-align: center;
            font-size: 16px;
        }
        
      </style>
  
</head>

<body>



<div class="container">
    
<p ><%=rows[0].name %></p>

<ul>
    
    <% rows.forEach(function(r) { %>
        name:<%= r.name %> <br>color: <%= r.color %> <br>width: <%= r.width %> <br> length:<%= r.length %> 
            <br> is it a square? <%= r.issquare %> <br> is it longer? <%= r.islonger %> <br> 
            is it wider? <%= r.iswider %><br>
    <% }); %>
</ul>
<form action="/delete-rectangle" method="post">
    <input type="hidden" name="name" value="<%=rows[0].name %>">
    <input type="submit"value="delete rectangle">
</form>
<form action="/changeRectanglePage" method="post">
    <input type="hidden" name="name" value="<%=rows[0].name %>">
    <input type = "submit" value="change rectangle">
</form>
<form action="/database" >
    <input type = "submit" value="Display All Rectangles">
</form>
</div>

</body>
</html>